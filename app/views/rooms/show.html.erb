<h1>チャット</h1>
<ul id="chat-index">
  <% @texts.each do |message| %>
  <li><%= message.text %></li>
  <% end %>
</ul>
<%= form_tag("/rooms/create_text") do %>
    <input id="chat-form" type="text" data-behavior="chat_post" name="message" value="入力">
    <%= hidden_field_tag :id, @room.id %>
  <% end %>

<br>
<%= link_to '戻る', root_path %>


<dl class="faq_area">
  <div>
    <dt>質問の内容が入ります。</dt>
    <dd>質問に対する答えの文章が入ります。</dd>
  </div>
  <div>
    <dt>質問の内容の文章が入ります。</dt>
    <dd>その答えの文章が入ります。</dd>
  </div>
  <div>
    <dt>質問です。</dt>
    <dd>答えが入ります。</dd>
  </div>
</dl>